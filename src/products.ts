export interface Product {
    name: string;
    price: number;
}

let products: Product[] = [
    { name: "Motor", price: 10.0 },
    { name: "Sensor", price: 12.5},
    { name: "LED", price: 1.0},
]

export function calcAverageProduct (array: Product[]): number {  //return type
 return 0; //to get rid of red

}

